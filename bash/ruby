# version manager: https://github.com/redding/rb#install
if command -v rb &>/dev/null; then
  eval "$(rb init --auto)"
fi

# gem completion
if [ -f ~/.bash/completion_scripts/gem-completion.rb ]; then
  complete -C ~/.bash/completion_scripts/gem-completion.rb -o default gem
fi
